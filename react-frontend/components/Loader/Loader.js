import React from "react";

export const Loader = () => (
  <div className="flex items-center content-center justify-center h-screen">
    <img src="/loading.png" width={208} alt={`loader`} className="opacity-10" />
  </div>
);
